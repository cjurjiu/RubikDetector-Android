<resources>

    <!-- Base application theme. -->
    <style name="TranslucentDecorNoActionBar" parent="android:Theme.Holo.Light.NoActionBar.TranslucentDecor">
        <!-- Customize your theme here. -->
    </style>

    <style name="NoActionBar" parent="android:Theme.Holo.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TransparentActionBarAppTheme" parent="android:Theme.Holo.Light">
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="actionBarStyle">@style/TransparentActionBarBackground</item>
        <item name="android:actionBarStyle">@style/TransparentActionBarBackground</item>

        <item name="windowActionBar">true</item>
        <item name="windowActionModeOverlay">true</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionOverflowButtonStyle">@style/MyActionButtonOverflow</item>
    </style>

    <style name="TransparentActionBarBackground" parent="android:Widget.Holo.Light.ActionBar.Solid">
        <item name="android:background">@android:color/transparent</item>
        <item name="background">@android:color/transparent</item>
    </style>

    <!-- Style to replace actionbar overflow icon. set item 'android:actionOverflowButtonStyle' in AppTheme -->
    <style name="MyActionButtonOverflow" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <!--<item name="android:background">?android:attr/actionBarItemBackground</item>-->
        <item name="android:background">#fff3f3f3</item>
    </style>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="android:Theme.Holo.Light.NoActionBar.TranslucentDecor">-->
    <style name="HoloLight" parent="android:Theme.Holo.Light">
        <!-- Customize your theme here. -->
    </style>

</resources>
